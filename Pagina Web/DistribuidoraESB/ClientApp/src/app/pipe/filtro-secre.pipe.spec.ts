import { FiltroSecrePipe } from './filtro-secre.pipe';

describe('FiltroSecrePipe', () => {
  it('create an instance', () => {
    const pipe = new FiltroSecrePipe();
    expect(pipe).toBeTruthy();
  });
});
